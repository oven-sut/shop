/**
 * X อ่าน `og:image` เป็นตัวสำรองอยู่แล้ว แต่ `twitter:image` ที่ประกาศตรง ๆ คือสิ่งที่
 * เครื่องมือตรวจการ์ดของ X รายงานว่าผ่าน — และการ์ดที่พรีวิวไม่ขึ้นในหน้าตรวจ คือ
 * การ์ดที่ไม่มีใครกล้าแชร์
 *
 * ใช้รูปเดียวกับ Open Graph: ขนาด 1200×630 กับ `summary_large_image` เป็นสัดส่วน
 * เดียวกัน จึงไม่มีเหตุให้วาดสองใบ
 */
export { default, alt, size, contentType } from './opengraph-image';
